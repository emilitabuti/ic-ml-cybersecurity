# _MEMORY Module Configuration — TEMPLATE
# ─────────────────────────────────────────────────────────────
# Repositório compartilhado: cada colaboradora possui sua config pessoal.
#
# COMO USAR:
#   1. Copie este arquivo para config.yaml (mesma pasta)
#   2. Substitua {seu-nome} pelo seu nome em minúsculas (emili | isabela | caroline)
#   3. config.yaml está no .gitignore — suas configs não serão sobrescritas pelo git
# ─────────────────────────────────────────────────────────────

user_name: {seu-nome}
communication_language: Portuguese
document_output_language: Portuguese
output_folder: "{project-root}/_bmad-output/{seu-nome}"
